[package]
name = "desktoplab-domain"
version = "0.1.0"
edition.workspace = true
license.workspace = true
rust-version.workspace = true
description = "DesktopLab core domain contracts and invariants."

[lib]
path = "src/lib.rs"

[dev-dependencies]
xtask = { path = "../../xtask" }
